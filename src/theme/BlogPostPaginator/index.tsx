/**
 * Swizzled to return null — the "You might also like" section in
 * PostFooterExtras already surfaces relevant follow-up content;
 * the default prev/next post navigator is redundant here.
 */
export default function BlogPostPaginator(): null {
  return null;
}
